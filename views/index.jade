extends layout

block content
  .container
    .row.center-text
      .span12
        h2 gitignore.io
        h6 Create gitignore files
    .row
      .span12
        .controls
          .span8
            input.input-block-level#ignoreSearch(type='hidden')
          button.btn.btn-primary.span3(type='submit', onClick='window.location="http://localhost:3000/api/"+$("#ignoreSearch").select2("val");') Generate
    .row
      .span12
        p CLI
        h5 OSX
        code sudo echo "function gi() { curl http://localhost:3000/api/$@ ;}"  >> ~/.profile
        h5 Linux
        code sudo echo "function gi() { curl http://localhost:3000/api/$@ ;}"  >> ~/.bash_profile